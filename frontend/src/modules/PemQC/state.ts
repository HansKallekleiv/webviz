import { EnsembleIdent } from "@framework/EnsembleIdent";
import { ColorScaleGradientType, ColorScaleType } from "@lib/utils/ColorScale";

export type State = { ensembleIdent: EnsembleIdent | null; wellName: string | null };
